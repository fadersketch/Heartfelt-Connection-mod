# Heartfelt-connection 机制详解（全量）

> 版本：v1.0.0 ｜ 适用：Minecraft 1.20.1 + Forge 47.4.21 ｜ 前置：TLM ≥1.5.0 + maidmarriage ≥2.0.0 + callresponse ≥2.0.0
> 本文档覆盖模组全部 92 个类（含 33 个 Mixin）的机制，按模块分组，不遗漏任何机制。

---

## 〇、总览：这个模组到底是什么

《心契誓约》(maidmarriage) 管结婚/家庭/女儿，《爱憎分明》(callresponse) 管信任/恐惧/背叛/饥饿，TLM 管女仆本体与好感。heartfelt 把它们缝成**一套完整的情感系统**：关系状态注入 AI 对话、信任恐惧冻结与好感折算、吃醋绝对压制、女仆主动告白（前摇走向）、玩家主动告白屏、女儿四阶段成长线、家庭保护、背叛悔改、伤害惩罚、纪念日、特殊奶、婚纱模型……全部通过三层机制实现（见 README「兼容原理」）：编译期零依赖 + 运行时反射、数据桥接读写双方存档、双层 Mixin 注入（required 24 个 + optional 9 个）。

### 三层关系语义（全模组判定中枢 RelationshipExemption）
- **isFrozen（自身冻结层）**：结婚 / 告白完成（恋人）/ 好感≥192（深爱未告白）/ 父女 任一成立 → 恐惧信任冻结、不背叛不淡忘、自己不争风吃醋；
- **isDedicated（恋爱宣告层）**：已婚 或 告白完成 → 主人名花有主，全体女仆不再吃醋（配合玩家持久标记 `heartfelt_dedicated` 绝对压制，不依赖在场检测）；
- **strictRelationKey（关系栏）**：只有确认关系（妻子/恋人/女儿）才返回标签注入 `<context>`，高好感暗恋不算。

---

## 一、情感引擎（affect 包，7 类）—— v1.5.98 移植 MaidSoulCore 精简版

独立于好感/心契/爱憎的 **PAD 三维情绪模型**（愉悦-唤醒-支配 + 亲密/冲突/受伤债/修复债/思慕），事件驱动、随事件变化、无事件时缓慢回归，情绪注入对话上下文让女仆说话带当下心情。

| 类 | 机制 |
|---|---|
| **AffectEventKind** | 情感事件枚举：OWNER_AFFECTION（喂食/送礼）、OWNER_APOLOGY（悔改安抚）、MAID_HURT_BY_OWNER（玩家攻击）、MAID_HURT_BY_WORLD（环境伤害）、OWNER_DISTRESS（主人死亡/哀悼）、OWNER_LONG_ABSENCE（长期不在）、CONFESSION_SUCCESS/FAILED、HEARTBROKEN、INTIMATE_INTERACTION（亲密互动）、HOLIDAY_CELEBRATION（纪念日）、QUIET_RECOVERY（静默恢复） |
| **AffectEngine** | 核心引擎：事件→多维脉冲（如 MAID_HURT_BY_OWNER：valence -0.22、conflict +0.24、hurtDebt +0.22；CONFESSION_SUCCESS：valence +0.24、positiveStreak +2）；stepTime 做静默回归（冲突/债衰减、思慕升向 0.42+intimacy×0.24）；stageBaseline 每阶段 VAD 基线 |
| **AffectProfile** | 连续情感快照，落女仆 ForgeData `heartfelt_affect`（收魂符/跨维度不丢）；维度 VAD（-1~1/0~1/0~1）+ intimacy/conflict/hurtDebt/repairDebt/longing（0~1）+ positiveEventStreak + relationshipStage + emotionLabel |
| **AffectStateManager** | 接入层：事件入口（onHurtByOwner 强度75 / onGift 55 / onIntimate 60 / onConfessionSuccess 85 / onConfessionFailed 70 / onHeartbroken 90）；每 5 秒对附近关系女仆做 QUIET_RECOVERY（冲突/债约 8 分钟平复）；`buildPromptBlock` 输出【当前情绪】段 |
| **EmotionLabel** | 主导情绪标签（JOY/TRUST/CONTENTMENT/EXCITEMENT/LOVE/ANTICIPATION/ANXIETY/SADNESS/ANGER/FEAR/NEUTRAL），由 VAD+亲密阈值推导，注入 LLM 提示词 |
| **RelationshipHmm** | 情感关系阶段推导：COURTING/SWEET/PASSIONATE/STABLE/COLD/REPAIRING——被打→COLD/REPAIRING；道歉/亲密/告白成功按 intimacy 与连续正向计数走 sweetOrStable；破裂→COLD；静默逐步回落 |
| **RelationshipStage** | 阶段枚举（区别于 maidmarriage 的确认关系，是"她此刻怎么看这段关系"的动态档位） |

---

## 二、关系判定核心（relationship 包，3 类）

| 类 | 机制 |
|---|---|
| **RelationshipExemption** | 全模组"关系/冻结/隔离"判定中枢（见总览三层语义）。isChild 判定：child_state_data.child()=true 且 father（或血统数据回退）== 主人 UUID；isMarried/isConfessed 读 maidmarriage TaskData；isPregnantOrPostpartum 读 pregnancy_data（改调官方 isInPostpartumRecovery，不再硬编码 72000 tick） |
| **FreezeConversion** | 冻结折算（v1.2.0）：确认关系后信任/恐惧数值冻结；玩家造成的信任变化等比折算为好感（信任+N→好感+N×1.0；恐惧+N→好感-N×0.5）；负向折算受每日上限（8）约束；非玩家来源只冻结不折算；好感跌破告白线 128 → 冻结解除 |
| **EmotionSmoothingManager** | 好感对信任/恐惧的"重力场"（v1.2.1）：未冻结女仆每 5 秒 信任+=rate×好感/384、恐惧-=rate×好感/384（rate 默认 0.2，满好感约 8 分钟从 0 拉满）——高好感自然趋向忠诚；冻结女仆跳过（防双重计算） |

---

## 三、跨模组兼容层（compat 包，6 类）—— 全部反射、安全降级

| 类 | 机制 |
|---|---|
| **ReflectUtil** | 全模组反射收敛点：类探活/方法句柄**只缓存成功**（失败下次重试，早期依赖未加载不永久降级）；所有 invoke 安全降级返回 null；method 缓存键含参数类型防同名重载碰撞；newInstance 按参数数量+装箱类型兼容匹配构造器 |
| **MaidMarriageCompat** | maidmarriage 适配层：① TaskData 六键解析（marriage_data / relationship_progress_data / child_state_data / child_lineage_data / pregnancy_data / mood_data，全部解析成功才缓存）；② record accessor 反射读取（readBool/readLong/readInt/readUuid，句柄按"声明类#方法名"缓存）；③ 心情入口 MoodManager.value/addMood（0~25）；④ 关系写入口 completeConfession / resetConfession / setMarried / clearMarriage / setChildState / clearChildState；⑤ 场景模拟 setPostpartum / setLonging / setLongingInteraction（思慕立即生效）；⑥ 亲密互动反射入口 ensureInteraction / isMaidInteracting / hugToggle / hugStop / kiss / lapStart / lapExit / pet；⑦ 女儿成长 ChildStage 枚举（INFANT→JUVENILE→CHILD→ADULT）+ stageTicksFor（按实时配置复刻 MaidChildEntity 阈值推导）+ releaseCarry（反射正规放下抱着状态） |
| **CallResponseCompat** | callresponse 适配层（真实包名 com.github.JumDa5he.callresponse.compat，1.0.0 曾误用 tartaricacid 全静默失效——A0 修复）：isBetraying / resetBetrayal / addFearFloat / addTrustFloat / emotionValues（信任/恐惧）/ actionTypeFlower / hungerOf（缺省 50）/ hungerSet（自带客户端同步）；未装优雅降级 |
| **PromaidCompat** | Promaid 配额桥：tryAcquire() 反射 com.maidsmart.dialogue.ApiQuotaManager——装了共享全局 LLM 配额（两模组合并计数），未装/反射失败放行（无配额限制）。零硬依赖 |
| **ChildGuardManager** | 孩子监护（v1.4.0）：魂符收妈妈→女儿（乘客或同 owner 孩子）强制坐下等妈妈（`heartfelt_waiting_mother` 标记 + 系统消息），妈妈回来解除并自动重新抱起；另提供 isTooSmall（婴儿/幼儿判定）被对话分发/气泡广泛调用 |
| **ChildActionReportManager** | 少女学习/探险归来汇报（检测 maidmarriage_child_action_mode 从非空变空瞬间发少女口吻气泡，按天轮换）+ 爸爸受伤即时关心（48 格内关系女儿按阶段发关心气泡，幼女旁白大哭/少女吓一跳/成女照顾，每女儿 5 分钟冷却）。只读 persistentData + Forge 事件，零跨模组依赖 |

---

## 四、对话与互动管理器（dialogue 包，11 类）

| 类 | 机制 |
|---|---|
| **DialogueDispatcher** | 对话公共分发器：isOwner（UUID 比较，跨维度不失效）/ maidsOf / isLLMReady（LLM_ENABLED+站点 enabled+密钥）/ isSafeArea（玩家与女仆半径内无敌对生物，告白与 Alt+J 共用）/ chatWithQuota（无 LLM/配额满/per-maid 开关 maid_smart_llm 关闭→发固定文本气泡并返回 false，幼儿兜底拦截）/ dramatize（LLM 剧情演绎，走全局配额） |
| **DialogueArbiter** | 主动说话仲裁器（v1.5.96）：多个关系女仆同时抢着说话时，每玩家同时只有一只在"主动开口"。三通道（低→高）：AMBIENT（日常/思慕）< INTIMATE（关系互动/女儿汇报）< PLAYER_REPLY（回应玩家动作，可打断前者）；同通道优先级=距离 0.3+关系 0.4+相识 0.3（妻子/恋人/女儿、近、久优先）。广播/家庭互动等一次性互动不上仲裁 |
| **ConfessionApproachManager** | 告白前摇状态机（v1.5.0）：女仆主动告白不突脸——威胁检查→系统消息"她似乎有什么想说的"→走向玩家→走到身边（≤2.0 格，须在 maidmarriage 交互门槛 2.25 内）→S2C 跳转 maidmarriage 告白选项界面；取消条件=死亡/离线/超时 60s/威胁；最小前摇间隔 50 tick；补记检测每 40 tick（accept 后补写 CONFESSION_AT/心情）。v1.5.387 修复时钟基准混用（gameTime vs server tick 计数导致老存档永远不触发）与 WALK_TARGET 被 idle 行为清空（双写导航 + 2 tick 刷新） |
| **MaidConfessionManager** | 女仆主动告白 + 关系破裂：好感>192 周期尝试（2 分钟窗口，baseChance 0.35 + favorBonus 0.60，满好感≈0.95）；已宣布关系（已婚/恋人）不尝试；威胁检查（24 格无敌对+不在战斗+主人 32 格内）→前摇；破裂监视（恋人好感跌破 128→resetConfession+心碎记忆+心情惩罚+冻结解除，其他暗恋者重新有机会）；handlePlayerConfession（玩家告白服务端判定，女儿/已婚/已告白被拒）；handleConfessionRejected（被拒→CONFESSION_FAILED 永久记忆+心情惩罚）；forceConfession（调试按钮，跳过概率/冷却） |
| **FamilyInteractionManager** | 家庭互动：三类 LLM 互动均纳入全局配额、每目标每类每天 1 次——① 母女互动（偶数日，妻子/女儿交替发声）；② 父女互动（奇数日，女儿按成长阶段说话）；③ 暗恋者感慨（主人已婚后的释然祝福）。成长事件：女儿阶段升级触发 LLM 小剧情+系统消息。伤心窗口内不参与 |
| **FamilyMourningManager** | 主人死亡反馈（v1.5.6 死亡调侃替代哀悼）：死亡瞬间推迟到下一 tick 发送（防吞消息），发送前核验真死（不死图腾救回不调侃），只取最近 5 只关系女仆防刷屏；婴儿/幼儿不调侃。v1.5.108 哀悼旧设计彻底删除 |
| **LongingEffectManager** | 思慕明显效果（v1.5.346）：扫描主人 24 格内思慕中关系女仆——心形粒子每秒 8 颗（原版 4 倍）+ 思慕气泡每 30 秒（妻子/恋人/女儿分文案，女儿按阶段轮换 LONGING 池）+ 系统消息每游戏日一次。触发：妻子/恋人=maidmarriage isLongingForInteraction；女儿=lastInteractionDay≥3 天（用父女向文案，绝不落恋爱向） |
| **RelationBroadcastManager** | 关系传播：女仆结成关系（恋人/妻子/女儿）后其他女仆知道并主动 LLM 对话（祝福/感慨/调侃）。每 20 秒扫描对比快照发现新增→触发；每女仆每日最多 1 次；纳入全局配额 |
| **PickupResponseManager** | 抱起回应（v1.5.345）：玩家抱起妻子/女儿/恋人（EntityMountEvent 钩子，TLM 抱起=骑乘）→LLM 沉浸式回应或降级固定气泡；仅确认关系女仆、伤心/哀悼不回应、每女仆 10 秒冷却 |
| **DialogueFreezeManager** | 对话选择性静止（v1.5.5）：Alt+J 对话面板打开期间其他生物定住（setNoAi），玩家与目标女仆可动；兜底恢复（登出/世界保存/停服/5 分钟超时）；每 2 秒补冻新生成生物；实体重进世界自动修复 NoAI 残留。调整器 GUI 同机制 |
| **WeddingDressModelManager** | 婚纱→花嫁酒狐模型（v1.5.360）：穿戴 maidmarriage 婚纱（胸甲槽）自动切 TLM 内置模型 geckolib:winefox_wedding，脱下还原；原模型 ID 存 `heartfelt_original_model`（重启不丢）；每 10 tick 扫描主人 48 格内女仆 |

---

## 五、战斗与惩罚（combat 包，4 类）

| 类 | 机制 |
|---|---|
| **AttackBetrayerBehavior** | 反击背叛女仆：仅战斗状态女仆（攻击类任务）主动攻击附近背叛女仆（先 8 格快探命中才 24 格全量，扫描间隔 40 tick）；自保逃跑中（`maid_smart_preserving`，与 Promaid 共享标记）不整改；优先级 150（低于自保 250） |
| **BetrayerAttackBrain** | IExtraMaidBrain：把反击行为注册进 TLM 女仆 Brain（优先级走配置默认 150） |
| **BetrayalRedemptionManager** | 背叛悔改（和解）：用**蛋糕**右击背叛女仆→每次减恐惧 8 + 增信任 4 + 累计进度，进度达标（3 次）且恐惧≤90 信任≥10→触发悔改：resetBetrayal + 重新认主 + LLM 悔改对话（走配额）+ 心情-5 + 清仇恨；中断超 3 天进度清零；首个安抚者记入 redemptor |
| **PlayerHarmPenaltyManager** | 玩家伤害惩罚（v1.4.1）：30 秒窗口内对同一冻结层女仆（妻子/恋人/女儿）累积 3 次伤害才确认故意（误伤豁免）→心情-5（每日上限 2）+ 伤心窗口（1 游戏日：强制坐下赌气、沮丧档 mood 钳制、拒绝对话、委屈文本注入）+ 系统消息/气泡（女儿专属文本）+ 可选 LLM 伤心对话；幼儿（INFANT/JUVENILE）：任何来源都哇哇大哭（无冷却），仅主人来源立即好感-1。v1.5.9 兼容 callresponse 中性源重放（hurt HEAD 记录玩家攻击还原身份） |

---

## 六、保护系统（protect 包，2 类）

| 类 | 机制 |
|---|---|
| **FamilyProtectionManager** | 家庭保护（LivingHurtEvent）：受害者是关系女仆→无条件豁免（背叛女仆除外）；攻击者好感≥64 且同主人→取消伤害（深爱女仆不打同主人女仆，防母女/家人互殴）；检查间接来源（箭/弹幕的射手）；近战路径由 EntityMaidDoHurtProtectMixin 兜底（callresponse 中性源重放时事件直接实体为 null） |
| **PregnancyProtectionManager** | 孕期/产后保护：每 5 秒扫描怀孕中或产后恢复期女仆——饥饿<25 提到 25（低于饥饿伤害阈值 9）、>85 压回 85（不触发吃撑伤害），防怀孕女仆被爱憎分明饥饿系统饿到濒死 |

---

## 七、记忆与提示词（memory + prompt 包，9 类）

### 事件历史与剧情记忆（memory）
| 类 | 机制 |
|---|---|
| **EventHistoryManager** | 事件历史系统（v1.3.0 P1）：heartfelt 自记关键事件注入 LLM prompt——首见（EVENT_FIRST_MEET）、首礼（EVENT_FIRST_GIFT）、告白发起方/完成时刻、救主（EVENT_SAVED_MASTER，女仆击杀攻击主人的敌人）、破裂史（EVENT_BREAKUP_COUNT）；含纪念日逻辑（LAST_ANNIVERSARY_DAY 防重复，里程碑与 Promaid 游标联动） |
| **RelationshipMaidContext** | 关系栏 context（id=relationship）：返回 strictRelationKey（wife/lover/daughter）注入 `<context>`；无确认关系返回空串自动隐藏（暗恋不是恋人） |
| **StoryMaidContext** | 剧情记忆栏 context（id=story）：把 maidmarriage 既定剧情状态（结婚/怀孕/告白/父女/丧子）作为独立 context 注入，与 Promaid 基础记忆解耦 |
| **StoryMemoryManager** | 剧情记忆拼接：婚姻/怀孕（含双胞胎、产后 30 日"刚生过宝宝"）/心情（含丧子哀悼）/告白/自身是孩子（按成长阶段四档文案）/告白被拒（带第 N 天）/关系破裂。全部软依赖，未装静默 |

### 提示词注入（prompt）
| 类 | 机制 |
|---|---|
| **SmartPromptAppender** | 提示词组装：无确认关系→FAVORABILITY_SCALE（4 级量表 0-63/64-191/192-383/384，避免 LLM 按 points/128 误判）；有确认关系→RELATIONSHIP_GUIDANCE + 关系专属准则段（女儿按阶段细分，含防乱伦铁律）；再追加 P1 事件历史、P2 情境、P3 后果文本弧、P4 情绪快照 |
| **SituationalPrompt** | 情境变量（v1.3.0 P2）：饥饿（<40 很饿/>85 刚吃饱）/时间昼夜/地点群系/天气/主人状态（血量<40 担心、食物<8 饿了）；全部软读取，未装 callresponse 时不写噪声 |
| **AftermathPrompt** | 后果文本弧（v1.3.0 P3）：伤心>心碎>告白被拒>悔改 四档相处模式指引——被主人打伤（委屈赌气需要哄）、告白被拒（绝口不提保持距离）、关系破裂（<7 天 vs ≥7 天两档）、和好后（愧疚加倍忠诚） |
| **ChatNameFilter** | 称呼换字（v1.5.42）：女仆所有系统消息/气泡里「主人」按关系替换——恋人=亲爱的、妻子=丈夫、女儿=爸爸；普通女仆维持「主人」。气泡出口由 ChatBubbleNameFilterMixin 全覆盖 |
| **PromptTexts** | 提示词与文案集中管理（1610 行文本池）：关系准则段、父女/成长/广播/家庭互动/悔改/纪念日/伤害惩罚/特殊奶/思慕/死亡调侃等全部文本与 LLM 提示词；女儿对话文本池按 阶段×选项×天 轮换 |

---

## 八、Mixin 层（33 个，两层注册）

### required 核心 16 个（注入失败会启动报错）
| Mixin | 机制 |
|---|---|
| EmotionFearExemptionMixin | 关系恐惧冻结：拦截 callresponse EmotionData.addFear/addFearFloat——冻结层女仆恐惧完全冻结；玩家造成的恐惧变化经 FreezeConversion 折算为好感；非玩家来源只冻结 |
| EmotionTrustExemptionMixin | 关系信任冻结：同上对称（addTrust/addTrustFloat），配对=完全冻结 |
| EmotionBetrayalIsolationMixin | 关系女仆永不背叛：拦截 triggerBetrayal；背叛女仆攻击优先级改写（主人>关系女仆>普通女仆）；被恨玩家 UUID+时间戳写入 ForgeData（HATED_PLAYER/HATED_AT），超期 7 天自动清除 |
| EmotionForgettingIsolationMixin | 关系女仆永不淡忘：拦截 triggerForgetting |
| EmotionDotingIsolationMixin | 吃醋隔离：拦截 attackNearbyMaids——攻击者是关系女仆不争风吃醋；或主人已有任一关系女仆整体隔离（HEARTFELT_DEDICATED 全局标记支撑） |
| EmotionDotingSelectActionMixin | 溺爱动作修正：冻结层女仆 selectAction 一律 FLOWER（送花），跳过 40% 偷主人/30% 翻箱子——"被宠坏女仆偷东西"只保留给未确认关系普通女仆 |
| EmotionDevotedMixin | 自动忠诚：冻结层女仆 isDevoted 恒 true（与冻结数值解耦）——获得 Devoted buff（攻击+5/移速+0.1）、自动攻击附近背叛女仆（16 格，60% 扒装备）、主人血量<10% 自动回血（冷却 1200 tick） |
| EntityMaidDoHurtProtectMixin | 家庭保护生效层：拦截 EntityMaid.doHurtTarget（所有女仆近战唯一汇聚点，早于 callresponse 重放）——攻击者是背叛女仆放行；受害者是关系女仆取消；同主人且攻击者好感≥64 取消 |
| SpecialMilkBucketMixin | 特殊奶增强：玩家饮用完成→生命回复 I + 找最近关系女仆发特殊系统消息 + 写记忆（Promaid 记忆桥可读）；v1.4.4 兼容 maidmarriage 2.3.0（注入目标改到父类 MilkBucketItem，运行时 instanceof 过滤） |
| MaidWorkManagerSkillRetentionMixin | 孩子专属技能成年后保留：出生女仆（born_maid 标记）无论成年与否保留附魔/酿药技能；防好感重置（成年女儿好感>64 写初始化标记跳过重置） |
| MaidSpiritTtsGuardMixin | 灵体禁语音包：TLM TTS 入口拦截，灵体（MaidSpiritEntity）不播语音包；文本对话不受影响 |
| MaidAmbientVoiceMixin | 婴儿/幼儿禁自主语音：getAmbientSound 返回 null + 捡拾声拦截——等效声音频率=0（v1.5.64 最后缺口） |
| AIChatOpenSafetyMixin | Alt+J 对话安全区：OpenMaidAIChatMessage 服务端 handle 拦截——isSafeArea 不通过则取消打开+系统提示 |
| EntityMaidHurtRecordMixin | 玩家攻击记录：EntityMaid.hurt HEAD 记录 maid→player+tick（配合惩罚系统在 callresponse 中性源重放下还原玩家身份）+ 幼儿被打同 tick 去重 + 情感引擎喂脉冲 |
| ChatBubbleNameFilterMixin | 气泡称呼换字全覆盖（ChatBubbleManager 统一出口，addTextChatBubble/addThinkingText/addLLMChatText/addChatBubble 全部重写） |
| HeartbrokenChatGateMixin | 伤心女仆拒绝 AI 聊天：Alt+J 打开面板时目标是伤心窗口内女仆→取消+系统提示（女儿专属文本） |

### required 客户端 8 个
| Mixin | 机制 |
|---|---|
| PlayerConfessionEntryMixin | 告白方向修正（v1.3.0 P0）：玩家在 maidmarriage 对话菜单点"表白"→拦截 choose()→打开 heartfelt PlayerConfessionScreen（玩家开口+女仆回应），吞掉 maidmarriage 剧本；UUID 找不到按女仆名兜底 |
| HugActionScreenPauseMixin | 告白/对话静止：不再强制全屏暂停（v1.5.17），改由服务端 DialogueFreezeManager 选择性静止；界面关闭发 ChatFreezePacket(false) 恢复 |
| AIChatScreenDramatizeMixin | LLM 剧情演绎入口：Alt+J 面板加"演绎剧情"按钮→服务端按共同记忆沉浸式演绎；面板开/关联动选择性静止 |
| ScreenWidgetAccessor | Screen protected 成员访问器（width/height/addRenderableWidget 打在声明处，防 @Shadow 继承成员解析崩溃） |
| MaidAttackCooldownMixin | 空手/方块攻击女仆修复：Better Combat 挥击后的 missTime 对女仆清零（原版攻击路径恢复） |
| MaidCheckFallbackMixin | 坐姿女仆 Alt+J 辅助瞄准：原准星判定失败时兜底视线前方 10 格内最近的所属女仆（v1.5.60 扩展到所有女仆，视线锥角 41°） |
| HugDialogueDaughterFrameMixin | 女儿对话选项专属文本：HugActionScreen 帧级拦截 currentFrame()——按 阶段×选项 替换女仆台词（聊天/讨好/讲笑话/摇曲柄/摸头/举高高/让妈妈抱/陪她说话），原版文本在女儿身上永不触发 |
| DialogueBoxCenteredMixin | 心契对话面板正文居中：拦截 drawWrappedScaledText——按像素宽度逐字符切行（MC 的 Font.split 对无空格中文不换行），每行居中、不溢出面板、不与立绘重叠（v1.5.386 重做） |

### optional 9 个（require=0，注入失败只警告不崩溃）
| Mixin | 机制 |
|---|---|
| HungerSpeedTierCacheMixin | 爱憎分明速度档缓存：档位没变+基础值仍是档位值→跳过每秒重复 clobber（修"边走边饿速度鬼畜"）；外部改走（如 promaid 挖矿倍率）自动放行 |
| LoveLoatheActiveQuotaMixin | Love Loathe 主动情绪对话纳入全局 API 配额（配额满静默跳过） |
| LoveLoatheWitnessQuotaMixin | Love Loathe 目睹死亡对话纳入全局 API 配额 |
| MaidWorkSittingMixin | 女儿坐着也能安排工作：resolveWorkBlockReason 返回 SITTING 时改为 NONE（与 Promaid 坐姿工作兼容） |
| SmartPromptMixin | 提示词运行时注入：拦截 LLMMessage.systemChat（两条设定路径的唯一汇聚点）追加关系准则段（v1.0.1 改 MixinExtras @WrapMethod，修 @ModifyArg 非法组合崩溃） |
| CallResponseHurtLimitMixin | 主人攻击限伤修复：callresponse 中性源重放绕过 TLM÷5 限伤——ModifyVariable 改重放伤害为 TLM 原版语义（÷5 封顶 2）；空手豁免 |
| HeartPactVoicePlaybackMixin | 心契语音包拦截：play() 统一出口拦截——灵体/幼年女儿不播放心契语音包（独立音频系统，TTS 拦截覆盖不到） |
| ChildGiftTextMixin | 女儿对话文本统一分流：拦截 componentForMaid（摸头/抱抱/送礼/学习/儿童互动全部汇聚点）——幼女旁白无台词/少女小女孩口吻/成女父女向/普通女仆原样 |
| CarryToggleSittingMixin | "让妈妈抱"坐姿放宽：三处 isMaidInSittingPose 检查恒 false——幼女（强制坐下）也能被抱起；骑乘检查保留 |

---

## 九、网络（network 包，1 类）

**HeartfeltNetwork**：`heartfelt_connection:main` 通道（协议版本 3），注册 9 个包：
1. OpenMaidMarriageConfessionPacket（S2C）：告白前摇完成→客户端记 maidmarriage 断点（confession_intro）；maidUuid=null 清除断点防残留误跳；
2. PlayerConfessionPacket（C2S）：玩家告白屏"说出心意"→服务端判定；
3. PlayerConfessionResultPacket（S2C）：回传女仆回应（接受/委婉+文本）；
4. DramatizePacket（C2S）：演绎按钮→LLM 剧情演绎；
5. OpenAdjusterPacket（S2C）：调整器状态快照→同女仆原地刷新；
6. AdjusterActionPacket（C2S）：调整器按钮动作（须手持调整器或 OP）；
7. ChatFreezePacket（C2S）：Alt+J 面板开/关→选择性静止（审计：只能对自己的女仆或 OP 发起冻结）；
8. ConfessionRejectPacket（C2S）：告白剧本选"先让我缓缓"→永久记忆+心情惩罚（审计：验证归属防他人封杀）；
9. ForceConfessionPacket（C2S）：手册"立即触发主动表白"调试按钮。

---

## 十、调整器测试工具链（item + client + command + debug）

- **AdjusterItem**（形象=纸，仅创造栏+指令获取，附魔光泽）：右击女仆→AdjusterInteractHandler（v1.4.5 补漏右击入口）阻止原版交互→打开 GUI；
- **AdjusterScreen**（v1.4.6 GUI 化）：两页布局（基础：好感/关系/心情/情绪/阶段/记忆/清理；高级：场景模拟/数值精度/实用/关系细节），打开时暂停世界，按钮点击即时黄色结果行反馈；
- **AdjusterManager**：按钮动作执行体——好感=TLM API；心情=MaidMarriageCompat；信任/恐惧=CallResponseCompat；关系=completeConfession/setMarried/clearMarriage/setChildState；成长阶段=stageTicksFor 推导；纪念日测试（首见=今天/7天前/重置/时间+3/+7/+30/+100 天快进推进 gameTime+dayTime+server tick 三时钟）；
- **HeartfeltCommand**：`/heartfelt adjust <uuid> <action>` 命令通道（手持调整器即权限）；
- **HeartfeltDebugApi**：供 Promaid 手册反射调用的调试 API（契约固定）：isLoaded/version/featureSummary（运行时探测 12 项功能 ON/OFF）/maidDebug（单女仆调试行：关系/阶段/好感/冻结/背叛/告白状态/心碎/悔改进度/仇恨玩家/怀孕产后/心情/信任恐惧/纪念日里程碑+Promaid 联动游标）/clearHatedPlayer/forceRedemption/resetConfessionFailure。

---

## 十一、配置（config 包，1 类）

**HeartfeltConfig**（Forge COMMON 配置，改动需重启生效），分组与默认值：
- **hatedPlayer**：retentionDays=7（仇恨标记留存游戏日）；
- **fearFavor**：dailyCap=8（恐惧扣好感每日上限）；
- **redemption**：feeds=3、fearDrop=8.0、trustGain=4.0、staleDays=3；
- **freeze**：confessionLine=128（恋人冻结线=告白线）、trustFavorRatio=1.0、fearFavorRatio=0.5；
- **confession**：attemptInterval=2400（2min 尝试窗口）、baseChance=0.35、favorBonus=0.60（满好感≈0.95）、threatRadius=24、maxDistance=32、failMood=6、requiredFavor=192、approachEnabled=true、approachDistance=2.0、approachTimeout=1200（60s）、approachSpeed=1.0、approachMinTicks=50、dialogueSafeRadius=32；
- **emotionSmooth**：ratePerSecond=0.2、batchTicks=100；
- **behavior**：brainPriority=150、scanInterval=40；
- **dialogue**：broadcastInterval=400、familyInterval=2400、pregnancyInterval=100、fatherDaughterEnabled=true、growthEventEnabled=true、playerConfessionEnabled=true；
- **harmPenalty**：triggerHits=3、moodDrop=5、dailyCap=2、feelingTicks=24000、llmReaction=true。

---

## 十二、配额（quota 包，1 类）

**ApiQuotaBridge**：全局 API 配额桥门面，实现委托 PromaidCompat.tryAcquire()（反射 Promaid 的 ApiQuotaManager）——装了 Promaid 两模组合并计数（默认 10 次/天），未装放行。被 DialogueDispatcher、LoveLoathe 两个配额 mixin、BetrayalRedemptionManager 统一使用。

---

## 十三、共享常量（tags 包，1 类）

**HeartfeltTags**：NBT 键/mod id/包名前缀统一收敛（杜绝裸字符串漂移）——依赖 id（MM_ID/CR_PACKAGE/MM_COMPAT）；背叛（hated_player/at）；悔改（redemption_progress/betrayed_at/redemptor/redempted/at）；告白与破裂（confession_failed/failed_at/last_confession_tick/heartbroken_at）；女儿成长（child_stage/at）；事件历史（confession_by/at、ev_first_meet/gift/saved_master/breakup_count/last_anniversary_day）；特殊奶（special_milk_at/count）；孩子监护（waiting_mother）；伤害惩罚（hurt_until/penalty_day/count）；思慕（longing_msg_day）；拾取/静音原值（pickup_orig/sound_orig）；与 Promaid 共享自保标记（maid_smart_preserving，值必须一致）；确认关系全局标记（heartfelt_dedicated）。

---

## 十四、入口类（根目录 2 类）

- **HeartfeltMod**：@Mod 入口——注册配置、注册物品（调整器）、初始化网络通道、客户端初始化；
- **HeartfeltExtension**：@LittleMaidExtension 被 TLM 自动发现——注册 14 个事件管理器（关系广播/家庭保护/孕期保护/哀悼/家庭互动/抱起回应/思慕效果/婚纱模型/背叛悔改/告白/情绪缓和/事件历史/孩子监护/伤害惩罚）+ 实体离场清理；注册 AI 工具 SmartIntimateTool、AI context（relationship 分类的 RelationshipMaidContext + StoryMaidContext）、ExtraMaidBrain（BetrayerAttackBrain 反击背叛）。

---

## 附：统计核对

92 个类全覆盖——根目录 2、affect 7、client 4、combat 4、command 1、compat 6、config 1、debug 1、dialogue 11、item 4、memory 4、mixin 33（required 16 core + 8 client，optional 9）、network 1、prompt 5、protect 2、quota 1、relationship 3、tags 1、tool 1。
